# C++ Raid Bot
## RUN
**Linux:**
```
( For Run )
bash scripts/run.sh

( For Compile )
bash scripts/build.sh
```

**Windows:**
> You need compile the EXE file.
```
( For Run )
bash scripts/run.bat

(For Compile)
bash scripts/build.bat
```